# 记账App编码规范

## 1. 项目结构

### 1.1 目录结构
```
app/src/main/java/com/yintianyan/tallybook/
├── components/          # 可复用UI组件
├── constants/           # 常量定义
├── model/               # 数据模型层
│   ├── dao/             # 数据访问对象
│   ├── database/        # 数据库相关
│   └── entity/          # 数据实体
├── routes/              # 路由和数据类型定义
├── screens/             # 页面组件
│   ├── homescreen/      # 首页相关
│   │   ├── view/        # UI组件
│   │   ├── viewmodel/   # 状态管理
│   │   └── ...          # 其他相关文件
│   ├── statistics/      # 统计页面
│   └── profile/         # 个人页面
├── theme/               # 主题相关
└── util/                # 工具类
```

### 1.2 包结构
- 按功能模块组织代码，每个模块包含其相关的UI、业务逻辑和数据处理
- 避免跨模块直接依赖，通过接口或事件传递数据

## 2. Kotlin编码规范

### 2.1 命名规范
- 类名：使用大驼峰命名法（PascalCase）
- 函数和变量：使用小驼峰命名法（camelCase）
- 常量：全部大写，单词间用下划线分隔（CONSTANT_CASE）
- 测试类：以Test结尾
- 测试函数：以测试方法名描述测试内容

### 2.2 代码格式
- 使用4个空格缩进
- 每行代码不超过100个字符
- 使用标准的Kotlin格式化工具（ktfmt）

### 2.3 注释规范
- 每个公共类和公共函数必须有KDoc注释
- 复杂逻辑需要添加内联注释说明
- 注释内容要简洁明了，解释为什么这样做，而不是做了什么

## 3. Jetpack Compose编码规范

### 3.1 组件设计原则
- 单一职责：每个组件只负责一个功能
- 无状态优先：优先使用无状态组件
- 组合优于继承：通过组合小的组件构建复杂界面
- 可复用性：设计时考虑组件的可复用性

### 3.2 状态管理
- 使用State和MutableState管理UI状态
- 将状态提升到合适的层级，避免状态分散
- 使用ViewModel管理UI相关的业务状态
- 避免在多个组件间传递过多的回调函数

### 3.3 Composable函数规范
- 参数顺序：required params -> optional params -> content -> modifier
- 为可选参数提供合理的默认值
- 避免在Composable函数中执行业务逻辑
- 使用Modifier参数时，确保其在函数内部被使用

## 4. 数据层规范

### 4.1 Room数据库
- 实体类使用@Entity注解，属性使用@ColumnInfo指定列名
- DAO接口使用@Dao注解，查询方法使用@Query注解
- 数据库类使用@Database注解，包含所有实体类

### 4.2 数据转换
- 在数据层和UI层之间使用数据转换函数
- 使用扩展函数实现Entity到Domain Model的转换
- 转换逻辑集中管理，避免在多处重复

## 5. 性能优化

### 5.1 Compose性能
- 使用LazyColumn和LazyRow处理大量数据
- 避免在重组时创建对象和Lambda表达式
- 使用remember保存不需要重新计算的值
- 合理使用derivedStateOf优化计算逻辑

### 5.2 内存管理
- 使用适当的生命周期感知组件
- 避免内存泄漏，及时释放资源
- 合理使用协程作用域，避免内存泄漏

## 6. 错误处理

### 6.1 异常处理
- 使用try-catch处理可能的异常
- 为用户提供友好的错误提示
- 记录必要的错误日志便于调试

### 6.2 数据验证
- 对用户输入进行验证
- 验证数据的有效性和完整性
- 提供适当的输入提示和错误反馈

## 7. 测试

### 7.1 单元测试
- 为业务逻辑编写单元测试
- 使用Mockito或MockK进行依赖模拟
- 测试边界条件和异常情况

### 7.2 UI测试
- 使用Espresso或Compose Testing进行UI测试
- 测试主要用户交互流程
- 覆盖各种状态和场景